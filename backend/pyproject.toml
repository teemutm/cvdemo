[project]
name = "cvdemo-backend"
version = "0.0.1"
description = "Teemu's CV site backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-dotenv"
]

[dependency-groups]
dev = [
    "fastapi",
    "python-dotenv"
]
